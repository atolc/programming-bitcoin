# Chapter 6: Script

Bitcoin Script is a *stack-based* language with no loops or persistent state that defines who can spend each output. It is deliberately not Turing-complete: its simplicity makes validation cost predictable and reduces the attack surface.
